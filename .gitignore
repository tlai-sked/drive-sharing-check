.DS_Store

# clasp credentials — never commit these
.clasprc.json

# Vercel's local project link. Machine-specific; the CLI recreates it.
.vercel

# Test tooling only. The tool itself has no dependencies.
node_modules/
